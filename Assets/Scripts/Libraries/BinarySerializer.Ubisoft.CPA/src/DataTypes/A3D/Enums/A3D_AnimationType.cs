namespace BinarySerializer.Ubisoft.CPA {
    public enum A3D_AnimationType {
		A3I, // Interpolated. The only animation type from TT Retail on
		A3B, // Binary A3D. No keyframes: transforms are defined per frame
	}
}